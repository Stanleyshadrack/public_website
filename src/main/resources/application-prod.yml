server:
  port: ${SERVER_PORT:9091}

spring:
  datasource:
    url: jdbc:postgresql://155.133.22.75:5432/website_application

    username: postgres
    password: smartcode
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  internal:
    key: ${X-INTERNAL-KEY:dev-key}